module.exports = {
    semi: false, // No semicolons at the end of statements
    singleQuote: true, // Use single quotes instead of double
    printWidth: 80, // Set a moderate line width
    tabWidth: 4, // Appears to be 4 spaces per indentation level based on the example
    useTabs: false, // Use spaces instead of tabs
    trailingComma: 'none', // No trailing commas
    bracketSpacing: true, // Spaces between brackets in object literals
    arrowParens: 'avoid' // Avoid parentheses around a single arrow function parameter
}
